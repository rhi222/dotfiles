# External Claude Code skills
# Requires: gh CLI >= v2.90.0 with `gh auth login` completed
# Lines starting with # are ignored; inline `# ...` comments allowed.
#
# Two line formats:
#   <OWNER/REPO> <skill-name>[@<version>]
#     Installs via `gh skill install` (remote). Needs the repo to be
#     registered in the agentskills.io index.
#
#   local: <git-url> <sub-path> <skill-name>
#     Falls back to git clone + `gh skill install --from-local` for repos
#     that are not yet indexed. <skill-name> is the SKILL.md `name:` value,
#     used for installed-directory dedup.

anthropics/skills frontend-design
anthropics/skills skill-creator
browser-use/browser-use browser-use
sanyuan0704/code-review-expert code-review-expert
vercel-labs/agent-browser agent-browser
local: https://github.com/vercel-labs/agent-skills.git skills/react-best-practices vercel-react-best-practices
local: https://github.com/vercel-labs/agent-skills.git skills/composition-patterns vercel-composition-patterns
