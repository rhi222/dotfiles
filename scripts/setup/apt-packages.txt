bubblewrap
# nvim が C をコンパイルする箇所に要る。nvim-treesitter のパーサ、
# kulala の kulala_http パーサ（tree-sitter build）、telescope-fzy-native の
# ネイティブライブラリ。無いとハイライトと絞り込みが黙って壊れる
build-essential
# ログインシェル。apt-setup.sh より前に手で入れる必要があるが、
# 宣言が無いと新環境で「何で入れたか」が追えなくなるのでここにも並べる
fish
graphviz
imagemagick
jq
# OpenSSL に link する cargo crate のビルドに要る（cargo-update など）。
# 無いと openssl-sys の configure が openssl.pc を見つけられず `cargo install` が落ちる
libssl-dev
pkg-config
plantuml
postgresql-client-16
postgresql-client-common
# lint.sh の YAML 検査。python3 本体は ubuntu に既定で入っているが PyYAML は
# 別パッケージで、無いと lint が YAML を skip して通す（CI で実際に踏んだ）
python3-yaml
# mise/aqua に tig が無い。GitHub リリースがソース tarball しか配っておらず
# ubi でも取れない（`tig-completion.bash has unknown extension` で落ちる）ため、
# CLI ツールの中でここだけ apt に寄せる
tig
unzip
wslu
zip
